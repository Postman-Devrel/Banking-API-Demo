#!/usr/bin/env sh
. "$(dirname -- "$0")/_/husky.sh"

echo "🔍 Running linter on staged files..."
npx lint-staged

echo "🧪 Running tests..."
npm test

# Run Postman tests only if API code changed
if git diff --cached --name-only | grep -qE "src/routes|src/middleware"; then
  echo "📬 API code changed - Running Postman tests..."
  postman collection run
else
  echo "⏭️  No API changes detected, skipping Postman tests"
fi

echo "✅ All pre-commit checks passed!"
