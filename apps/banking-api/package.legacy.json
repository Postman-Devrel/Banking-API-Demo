{
  "name": "intergalactic-bank-api",
  "version": "1.0.0",
  "description": "REST API for Intergalactic Bank - Managing accounts and transactions",
  "main": "src/server.js",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "test": "jest --coverage",
    "test:watch": "jest --watch",
    "test:unit": "jest tests/unit",
    "test:integration": "jest tests/integration",
    "test:api": "for file in postman/collections/*.postman_collection.json; do echo \"Running $file...\"; postman collection run \"$file\"; done",
    "lint": "eslint .",
    "lint:openapi": "spectral lint --ruleset .spectral-openapi.yaml openapi/openapi.yaml",
    "build:openapi": "node scripts/build-openapi.js",
    "check:openapi": "node scripts/check-openapi.js",
    "verify": "npm run build:openapi && npm run check:openapi && npm run lint:openapi && npm run lint && npm test -- --runInBand",
    "lint:fix": "eslint . --fix",
    "lint:collections": "node .spectral/lint-with-names.js",
    "prepare": "husky install"
  },
  "keywords": [
    "banking",
    "api",
    "rest",
    "express"
  ],
  "author": "",
  "license": "ISC",
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "ajv": "^8.17.1",
    "ajv-formats": "^3.0.1",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "json-canonicalize": "^1.2.0",
    "yaml": "^2.8.1"
  },
  "devDependencies": {
    "@stoplight/spectral-cli": "^6.11.0",
    "eslint": "^8.57.0",
    "eslint-plugin-jest": "^27.9.0",
    "eslint-plugin-node": "^11.1.0",
    "husky": "^8.0.3",
    "jest": "^29.7.0",
    "lint-staged": "^15.2.0",
    "nodemon": "^3.0.1",
    "postman-cli": "^1.3.0",
    "supertest": "^6.3.3"
  },
  "lint-staged": {
    "*.js": [
      "eslint --fix"
    ],
    "postman/collections/*.postman_collection.json": [
      "spectral lint"
    ]
  }
}
